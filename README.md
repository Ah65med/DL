# DL
Credit card fraud detection using a PyTorch Autoencoder + XGBoost ensemble. The Autoencoder flags anomalies via reconstruction error; XGBoost handles final classification. FastAPI backend, Streamlit dashboard, PostgreSQL, Docker Compose. Built as a Deep Learning course project.
